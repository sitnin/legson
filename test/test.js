"use strict";

const should = require('should');

describe("Class", () => {
    it("creates an object with defaults", () => NIY());
    it("creates an object with overrides", () => NIY());
});

describe("Configuration options", () => {
    describe("maxValueLength", () => {
        it("checks 250 by default", () => NIY());
        it("checks only 50", () => NIY());
        it("checks only 10", () => NIY());
    });

    describe("nullNonExistent", () => {
        it("when true", () => NIY());
        it("when false", () => NIY());
    });
});

describe("Loaders", () => {
    describe("Bundled", () => {
        it("json", () => NIY());
        it("markdown", () => NIY());
        it("url", () => NIY());
    });

    describe("Additional", () => {
        it("works", () => NIY());
    });
});

describe("Methods", () => {
    describe("load", () => {
        it("works", () => NIY());
    });
});

function NIY () {
    throw new Error("Not implemented, yet");
}
