"use strict";

const LegSON = require("../");

const should = require('should');

describe("Methods", () => {
    beforeEach(() => {
        this.l = new LegSON();
    });

    describe("load", () => {
        it("works");
        it("throws when root file not found");
    });
});
